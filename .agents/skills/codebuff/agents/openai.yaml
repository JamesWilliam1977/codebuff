interface:
  display_name: "Codebuff"
  short_description: "Repo-specific patterns and workflows for codebuff"
  default_prompt: "Use the codebuff repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true